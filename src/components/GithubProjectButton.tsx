import * as React from 'react';
import {
  memo,
} from 'react';

export const GithubProjectButton: React.FunctionComponent = memo(() => {
  return (
    <button type="submit">
      Найти
    </button>
  );
});
